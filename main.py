from handle_image import HandleImage
from read_contents import ReadContents

img_path = "calender.png"
min_length = 40
row, column = [6, 7]
handle_image = ReadContents(img_path, min_length, row, column)